name: flutter_firebase_android_setup
description: A new Flutter project.

publish_to: 'none'

version: 1.0.0+1

environment:
  sdk: '>=2.12.0 <3.0.0'

dependencies:
  flutter:
    sdk: flutter


  cupertino_icons: ^1.0.2

  ## sign in
  firebase_auth: ^1.1.3
  ## online database
  cloud_firestore: ^2.0.0

  firebase_core: ^1.1.0


dev_dependencies:
  flutter_test:
    sdk: flutter


flutter:

  uses-material-design: true
